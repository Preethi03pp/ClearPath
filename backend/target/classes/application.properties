# Backend port
server.port=${PORT:8080}

# MongoDB URI (cloud MongoDB)
spring.data.mongodb.uri=${MONGO_URI:mongodb+srv://username:password@cluster0.mongodb.net/dbname}

# JWT secret
app.jwt.secret=${JWT_SECRET:dev-secret-change-me-at-least-32-chars}

# CORS allowed origins (frontend Netlify URL)
app.cors.allowedOrigins=${CORS_ORIGINS:https://clearpathfront.netlify.app}

# File upload directory
app.upload.dir=${UPLOAD_DIR:uploads}

# Multipart file limits
spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=25MB
